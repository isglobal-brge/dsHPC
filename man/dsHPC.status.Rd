% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsHPC.R
\name{dsHPC.status}
\alias{dsHPC.status}
\title{Get Job Status}
\usage{
dsHPC.status(job_id, return_result = FALSE)
}
\arguments{
\item{job_id}{Character string with the job ID.}

\item{return_result}{Logical indicating whether to retrieve the result if completed.}
}
\value{
A list containing job information including status and result (if available and requested).
}
\description{
Checks the current status of a job and updates the database.
}
\examples{
\dontrun{
# Initialize dsHPC first
dsHPC.init()

# Submit a job
job <- dsHPC.submit_by_name(
  func_name = "mean",
  args = list(x = c(1, 2, 3, 4, 5))
)

# Check job status
status <- dsHPC.status(job$job_id)

# Check status and get result if available
result <- dsHPC.status(job$job_id, return_result = TRUE)
}
}
