% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{dsHPC.submit_python}
\alias{dsHPC.submit_python}
\title{Python Integration Functions for dsHPC}
\usage{
dsHPC.submit_python(
  py_module,
  py_function,
  args = list(),
  python_path = NULL,
  virtualenv = NULL,
  condaenv = NULL,
  slurm_opts = list(),
  use_cache = TRUE,
  required_modules = NULL
)
}
\arguments{
\item{py_module}{Character string with the name of the Python module containing the function.}

\item{py_function}{Character string with the name of the Python function to execute.}

\item{args}{List of arguments to pass to the Python function.}

\item{python_path}{Optional character string with the path to the Python executable.}

\item{virtualenv}{Optional character string with the name of the virtualenv to use.}

\item{condaenv}{Optional character string with the name of the conda environment to use.}

\item{slurm_opts}{Optional list with Slurm-specific options (partition, memory, time, cpus).}

\item{use_cache}{Logical indicating whether to reuse cached results.}

\item{required_modules}{Character vector with additional Python modules to import.}
}
\value{
List with job information.
}
\description{
This file contains functions for submitting and managing Python jobs
through the reticulate package.

Submit a Python function to be executed via reticulate.
}
\examples{
\dontrun{
# Initialize dsHPC first
dsHPC.init()

# Submit a Python job using numpy
job <- dsHPC.submit_python(
  py_module = "numpy",
  py_function = "mean",
  args = list(a = c(1, 2, 3, 4, 5))
)

# Check job status
status <- dsHPC.status(job$job_id)

# Get result when job is complete
if (status$status == "COMPLETED") {
  result <- dsHPC.result(job$job_id)
  print(result)
}
}
}
