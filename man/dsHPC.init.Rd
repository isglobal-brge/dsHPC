% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsHPC.R
\name{dsHPC.init}
\alias{dsHPC.init}
\title{dsHPC: DataSHIELD Interface for High-Performance Computing}
\usage{
dsHPC.init(db_path = NULL, scheduler = "slurm")
}
\arguments{
\item{db_path}{Optional character string specifying the path to the SQLite database file.
If NULL (default), uses the package's default location in the user's home directory.}

\item{scheduler}{Character string specifying the job scheduler to use. Currently only "slurm"
is supported.}
}
\value{
A list containing configuration details and connection objects.
}
\description{
This package provides a server-side interface for DataSHIELD packages to submit
and manage jobs on High-Performance Computing clusters. It supports caching results to avoid
redundant computations and provides tools for job lifecycle management.

Sets up the dsHPC system by initializing the database connection and checking
for available job schedulers.
}
\examples{
\dontrun{
# Initialize with default settings
dsHPC_config <- dsHPC.init()

# Initialize with a custom database path
dsHPC_config <- dsHPC.init(db_path = "/path/to/custom/database.sqlite")
}
}
