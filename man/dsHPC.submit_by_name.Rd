% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsHPC.R, R/jobs.R
\name{dsHPC.submit_by_name}
\alias{dsHPC.submit_by_name}
\title{Submit a Function Execution as a Job by Name}
\usage{
dsHPC.submit_by_name(
  func_name,
  args = list(),
  object_hash = NULL,
  slurm_opts = list(),
  use_cache = TRUE,
  required_packages = NULL
)

dsHPC.submit_by_name(
  func_name,
  args = list(),
  object_hash = NULL,
  slurm_opts = list(),
  use_cache = TRUE,
  required_packages = NULL
)
}
\arguments{
\item{func_name}{Character string with the name of the function to run.}

\item{args}{List of arguments to pass to the function.}

\item{object_hash}{Optional pre-computed hash for the function and arguments.}

\item{slurm_opts}{Optional list with Slurm-specific options (partition, memory, time, cpus).}

\item{use_cache}{Logical indicating whether to reuse cached results.}

\item{required_packages}{Character vector with additional packages to load.}

\item{package}{Character string with the package name containing the function.}

\item{cache_only}{Logical indicating whether to only check the cache without submitting a job (default: FALSE).}
}
\value{
A list containing job information including job_id, status, and result (if available from cache).

List with job information.
}
\description{
Submits a function to be executed as a job on the HPC cluster using the function name.

Submit a job by providing a function name and arguments.
}
\examples{
\dontrun{
# Initialize dsHPC first
dsHPC.init()

# Submit a simple job
job <- dsHPC.submit_by_name(
  func_name = "mean",
  args = list(x = c(1, 2, 3, 4, 5))
)

# Submit a job with package function and custom Slurm options
job <- dsHPC.submit_by_name(
  func_name = "kmeans",
  args = list(x = my_data_matrix, centers = 3),
  package = "stats",
  required_packages = c("stats"),
  slurm_opts = list(
    partition = "normal",
    memory = "8G",
    time = "02:00:00",
    cpus = 2
  )
)
}
\dontrun{
# Initialize dsHPC first
dsHPC.init()

# Submit a job
job <- dsHPC.submit_by_name(
  func_name = "mean",
  args = list(x = c(1, 2, 3, 4, 5))
)

# Check job status
status <- dsHPC.status(job$job_id)

# Get result when job is complete
if (status$status == "COMPLETED") {
  result <- dsHPC.result(job$job_id)
  print(result)
}
}
}
