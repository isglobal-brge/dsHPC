% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsHPC.R
\name{dsHPC.submit}
\alias{dsHPC.submit}
\title{Submit a Function Execution as a Job}
\usage{
dsHPC.submit(
  func_name,
  args,
  package = NULL,
  object_hash = NULL,
  required_packages = NULL,
  use_cache = TRUE,
  cache_only = FALSE,
  slurm_opts = list()
)
}
\arguments{
\item{func_name}{Character string with the name of the function to execute.}

\item{args}{List of arguments to pass to the function.}

\item{package}{Character string with the package name containing the function.}

\item{object_hash}{Optional character string with a hash identifying a data object.}

\item{required_packages}{Character vector with names of additional packages to load.}

\item{use_cache}{Logical indicating whether to check for cached results (default: TRUE).}

\item{cache_only}{Logical indicating whether to only check the cache without submitting a job (default: FALSE).}

\item{slurm_opts}{List of options for Slurm job submission (partition, memory, time, cpus, etc.).}
}
\value{
A list containing job information including job_id, status, and result (if available from cache).
}
\description{
Submits a function to be executed as a job on the HPC cluster.
}
\examples{
\dontrun{
# Initialize dsHPC first
dsHPC.init()

# Submit a simple job
job <- dsHPC.submit(
  func_name = "mean",
  args = list(x = c(1, 2, 3, 4, 5))
)

# Submit a job with package function and custom Slurm options
job <- dsHPC.submit(
  func_name = "kmeans",
  args = list(x = my_data_matrix, centers = 3),
  package = "stats",
  required_packages = c("stats"),
  slurm_opts = list(
    partition = "normal",
    memory = "8G",
    time = "02:00:00",
    cpus = 2
  )
)
}
}
